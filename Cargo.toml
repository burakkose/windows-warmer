[package]
name = "warmer"
version = "0.1.0"
edition = "2021"
description = "A tiny native Windows tray app for warmer display color on systems where Night Light/f.lux fail."
license = "MIT"
build = "build.rs"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

